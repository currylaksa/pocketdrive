# Copy to .env (or just export in your shell) to enable the LIVE Claude Eco-Coach.
# Without it, the coach falls back to high-quality canned answers so the demo never breaks.
# Get a key at https://console.anthropic.com/  — NEVER commit a real key.
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
